# کنترل Agent در کار مهندسی

اصل: تو مالک نیازمندی، تصمیم و پذیرش خروجی هستی. Agent پیشنهاد و کد تولید می‌کند؛ توانایی تو با تولید آن اندازه‌گیری نمی‌شود.

## چرخه هر تغییر

۱. پیش از درخواست کد، مسئله و ۳ تا ۵ معیار پذیرش را خودت بنویس؛ حداقل یک failure case اضافه کن.
۲. از Agent بخواه جریان موجود، فایل‌های مرتبط و طرح کوتاه تغییر را توضیح دهد. برای کشف کد، ابزار graph پروژه اولویت دارد؛ اگر پروژه index نشده ابتدا index_repository، و در نبود ابزار/نتیجه کافی جست‌وجوی متنی با بیان محدودیت.
۳. فقط یک رفتار قابل بازبینی سفارش بده. حد پیشنهادی آموزشی: ۳ تا ۵ فایل و حدود ۲۰۰ خط تغییر منطقی؛ این سقف قانون همیشگی نیست، معیار اصلی فهم diff در حدود ۳۰ دقیقه است. از این حد گذشت، قابلیت را به مراحل قابل تست بشکن.
۴. قبل از اجرا پیش‌بینی کن کدام تست باید fail و پس از اصلاح pass شود. تست حیاتی را از روی نیازمندی خودت طراحی کن، نه فقط با خواندن پیاده‌سازی Agent.
۵. diff را بخوان و مسیر درخواست، قواعد دامنه، query، مجوز و رفتار خطا را دنبال کن. کدی که نمی‌توانی توضیح بدهی آماده پذیرش نیست.
۶. build و تست متناسب را اجرا کن؛ خروجی واقعی، commit یا مسیر فایل و نتیجه را ثبت کن. «Agent گفت پاس شده» شاهد اجرای تست نیست.
۷. رفتار خراب، مرزی و هم‌زمان را متناسب با تغییر بررسی کن؛ سپس تغییر کوچک را ثبت کن و دانش جلسه را بدون Agent توضیح بده.

## قالب سفارش

```text
جلسه: W...D...
مسئله و رفتار مورد انتظار:
معیارهای پذیرش و سناریوی شکست:
دامنه مجاز فایل‌ها/قابلیت:
محدودیت‌ها: قرارداد API، سازگاری داده، امنیت، dependency جدید
ابتدا مسیر موجود و طرح تغییر را توضیح بده، سپس یک تغییر کوچک اجرا کن.
پس از کار: خلاصه diff، دلیل تصمیم، تست‌های اجراشده با نتیجه واقعی،
موارد اجرا نشده، ریسک باقی‌مانده و روش برگشت را گزارش کن.
```

## چک بازبینی

- قرارداد: ورودی/خروجی و status code، validation و سازگاری مصرف‌کننده.
- داده: SQL واقعی، constraint، transaction، pagination و concurrency.
- امنیت: authentication در برابر authorization، مالکیت، secrets، خطا و log.
- طراحی: مسئولیت‌ها، lifetime، coupling، abstraction ضروری و dependency جدید.
- قابلیت اطمینان: cancellation، timeout و رفتار شکست؛ retry فقط با توجه به عوارض تکرار.
- شواهد: تست واقعاً اجرا شده، سناریوی منفی، داده/محیط مشخص، محدودیت صریح.

حد کار هم‌زمان در این دوره: یک قابلیت فعال؛ تا review آن تمام نشده قابلیت بعدی آغاز نمی‌شود. برای آموختن مفاهیم جدید، اولین نمونه کوچک و آزمون نهایی را بدون تولید کد Agent انجام بده؛ سپس از Agent برای گسترش، توضیح و review استفاده کن. داوری دوم Agent می‌تواند کمک کند اما جای تست یا درک مستقل را نمی‌گیرد.

## شاخص‌های هفتگی

زمان فهم diff، تعداد خطاهای کشف‌شده با تست مستقل، دفعات بازکاری، و توانایی توضیح جریان کد را ثبت کن. تعداد خطوط تولیدشده یا سرعت اولین پاسخ شاخص یادگیری و کیفیت نیست. سرعت مطلوب، زمان رسیدن به تغییر درست و قابل نگهداری است.
