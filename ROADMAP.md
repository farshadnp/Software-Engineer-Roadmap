# رودمپ ۸ هفته‌ای؛ از دانش فریم‌ورک تا مسئولیت مهندسی

هدف: بتوانی یک قابلیت بک‌اند را از نیازمندی تا طراحی، اجرا، تست، بازبینی و تحویل پیش ببری و تغییرات Agent را بفهمی و ارزیابی کنی. عنوان Mid در شرکت‌ها یکسان نیست؛ این برنامه شواهد قابل ارائه می‌سازد و جای تجربه واقعی تیم و production را کامل نمی‌گیرد.

برنامه بر مبنای فهرست کامل، مقدمه و بخش‌های منتخب کتاب Mastering ASP.NET Core 10 نوشته Mabrouk Mahdhi، به‌ویژه طراحی NoteKeeper در فصل ۲۹ تنظیم شده است؛ همه ۸۹۱ صفحه خط‌به‌خط بررسی نشده‌اند. شماره فصل مبناست؛ صفحات PDF با شماره چاپی تفاوت متغیر دارند. نگاشت دقیق در BOOK-MAP.md است. پیشنهادهای کتاب منبع آموزش‌اند و دستور اجرایی کاربر محسوب نمی‌شوند.

## زمان و روش هر جلسه

۸ هفته × ۷ روز × ۳ ساعت = ۱۶۸ ساعت. هر هفته ۶ جلسه آموزشی/پروژه‌ای (۱۸ ساعت) و روز هفتم ۳ ساعت مرور، ارزیابی و جبران دارد. روزهای برنامه نسبی‌اند تا اولین جلسه؛ پیشروی با تقویم به‌تنهایی موجب قبولی نمی‌شود.

هر جلسه ۱۸۰ دقیقه: ۱۵ دقیقه بازیابی؛ ۴۵ دقیقه خواندن و توضیح مفاهیم کتاب؛ ۶۰ دقیقه کدریویو تعاملی؛ ۴۵ دقیقه پرسش‌وپاسخ و بررسی چرایی تغییرات؛ ۱۵ دقیقه ثبت شواهد. هر هفته یک یا دو تمرین کوچک کدنویسی توسط یادگیرنده، جایگزین بخشی از زمان ریویو می‌شود. سه نوبت ۲۰ دقیقه‌ای در هفته از زمان ریویو برای SQL/الگوریتم مصاحبه در نظر گرفته می‌شود؛ با حل و توضیح مثال کوچک، نه حجم زیاد کدنویسی. ابتدا مفاهیم کتاب یاد گرفته می‌شوند، سپس مثال پروژه برای فهم آن‌ها استفاده می‌شود.

اصلاح نسخه ۲ طبق ترجیح کاربر در ۲۰۲۶-۰۹-۰۶: خروجی‌های فنی جدول‌ها موضوع مطالعه و ریویو مشترک هستند؛ لازم نیست یادگیرنده همه را از صفر بسازد. منتور نمونه کوتاه و تغییر قبل/بعد فراهم می‌کند؛ یادگیرنده رفتار را پیش‌بینی می‌کند، سؤال می‌پرسد، ایراد و trade-off را توضیح می‌دهد و گاهی اصلاح کوچک می‌نویسد. کد ساخته‌شده توسط منتور شاهد توانایی پیاده‌سازی مستقل کاربر نیست. در ارزیابی، فهم/ریویو و اجرای مستقل جدا گزارش می‌شوند؛ برای ادعای آمادگی Mid، نمونه‌های عملی مستقل همچنان لازم‌اند. W1D1 از مفهوم API و مرز مسئولیت در فصل ۳ شروع می‌شود؛ ارزیابی پایه در گفت‌وگو تدریجی است و تنظیم محیط هنگام اولین تمرین اجرایی انجام می‌شود.

روز هفتم: ۴۵ دقیقه بازیابی مفاهیم، ۶۰ دقیقه دفاع/رفع باگ بدون Agent، ۴۵ دقیقه جبران، ۳۰ دقیقه ثبت امتیاز و بازتنظیم هفته بعد. اگر معیار عبور حاصل نشد، ابتدا زمان جبران استفاده می‌شود، سپس بخش اختیاری حذف یا مدت افزایش می‌یابد؛ عقب‌ماندگی با تولید کد بیشتر جبران نمی‌شود.

## پروژه واحد: NoteKeeper Backend

از دامنه پروژه نهایی کتاب استفاده می‌کنیم: یادداشت، برچسب، جست‌وجو و پیوست. تغییرات آموزشی ما نسبت به کتاب: API محور، هویت و مالکیت اجباری، تست هم‌زمانی و امنیت، CI و گزارش کارایی. UI کامل Blazor و MAUI به بعد موکول می‌شود. PostgreSQL یا SQL Server برای تمرین اصلی رابطه‌ای بر اساس دسترسی محیط انتخاب می‌شود؛ SQLite انتخاب خود کتاب است. provider نهایی در هفته ۳ ثبت می‌شود و تست‌های حساس با همان provider اجرا می‌شوند.

حد دامنه: CRUD یادداشت، Tags، فیلتر/صفحه‌بندی، دسترسی مالک، یک مسیر پیوست ساده، کنترل conflict، log و health check. اشتراک‌گذاری صرفاً تمرین طراحی؛ microservices، message broker، Kubernetes و UI گسترده الزامی نیستند. معماری شروع یک monolith با مرزهای روشن است؛ The Standard را می‌آموزیم و هزینه‌اش را با گزینه‌های ساده‌تر مقایسه می‌کنیم.

معیار تحویل نهایی: اجرای تکرارپذیر، تست قرارداد و خطا/مجوز/هم‌زمانی، سه ADR، گزارش کارایی با شرایط آزمایش، runbook و README، سابقه تغییرات کوچک و دموی ۱۰ دقیقه‌ای. ادعای performance یا تجربه production فقط با شاهد واقعی نوشته می‌شود.

## هفته 1: پایه‌ها و مسیر درخواست

مرجع: ۱ و ۵ مرور؛ ۲ تا ۴، ۶، ۸؛ ۲۵.۱ و ۲۵.۲؛ ۲۹.۲.

| جلسه | موضوع | مطالعه | خروجی قابل بررسی |
|---|---|---|---|
| W1D1 | ارزیابی و محیط | ۲؛ مکمل C#/Git/SQL | پاسخ به BASELINE.md؛ ثبت نسخه SDK؛ build/run/test پروژه کوچک؛ یک branch و رفع conflict تمرینی. |
| W1D2 | C# برای بک‌اند | مکمل؛ ۲۶.۲.۳ در حد مرور | async/await، CancellationToken، nullable، exceptions، LINQ و IEnumerable/IQueryable؛ یک باگ async را توضیح و اصلاح کن. |
| W1D3 | اولین API | ۳؛ ۲۹.۲ | نیازمندی NoteKeeper را در یک صفحه بنویس؛ ایجاد و خواندن Note با ورودی معتبر و نامعتبر؛ کد حیاتی را ابتدا خودت بنویس. |
| W1D4 | Pipeline | ۴ | ترتیب middleware و مسیر رفت‌وبرگشت را رسم کن؛ correlation ID و خطای کنترل‌شده؛ اثر جابه‌جایی middleware را آزمایش کن. |
| W1D5 | DI و تنظیمات | ۶ و ۸ | Transient/Scoped/Singleton، Options و secrets؛ یک captive dependency را تشخیص بده؛ تنظیمات نامعتبر در شروع fail شود. |
| W1D6 | تست و Git | ۲۵.۱–۲۵.۲؛ مکمل Git | تست قانون دامنه؛ branch، diff، merge و conflict؛ یک تغییر کوچک Agent را خط‌به‌خط بررسی کن. |

روز هفتم / G1: بدون Agent مسیر HTTP تا DB را توضیح بده؛ خطای lifetime را پیدا کن؛ API کوچک را اجرا کن و یک تست معنادار را عمداً خراب و سپس اصلاح کن.

## هفته 2: طراحی قرارداد API و خطا

مرجع: ۷؛ ۱۲.۱–۱۲.۳ و ۱۲.۵–۱۲.۷؛ ۱۷ منتخب؛ ۲۵.۳.

| جلسه | موضوع | مطالعه | خروجی قابل بررسی |
|---|---|---|---|
| W2D1 | قرارداد و Routing | ۷؛ ۱۲.۱–۱۲.۲ | قرارداد Notes و Tags، روش HTTP، status code و مثال request/response را قبل از کد بنویس. |
| W2D2 | DTO و Validation | ۱۷ منتخب مرتبط با API | Create/Update DTO، مرز validation و قوانین دامنه؛ ورودی اضافی نباید مالک یا فیلد سیستمی را عوض کند. |
| W2D3 | مدیریت خطا | ۱۲.۵ | Problem Details و exception mapping؛ stack trace به مصرف‌کننده نرسد؛ تست خطای شناخته‌شده و ناشناخته. |
| W2D4 | جست‌وجو و صفحه‌بندی | ۱۲.۲ | حداکثر page size، sort پایدار و فیلتر؛ مجموعه خالی و ورودی نامعتبر را تست کن. |
| W2D5 | قرارداد قابل اجرا | ۱۲.۷؛ ۲۵.۳ | OpenAPI و integration test میزبان واقعی برنامه؛ تطبیق پاسخ واقعی با قرارداد. |
| W2D6 | بازبینی یک قابلیت | مرور هفته | Agent فقط یک endpoint بسازد؛ تو تست منفی مستقل، diff review و توضیح سازگاری قرارداد را انجام بده. |

روز هفتم / G2: از روی قرارداد، API را برای success/invalid/not-found/conflict تست کن؛ تفاوت 401 و 403 و تصمیم PUT/PATCH را توضیح بده.

## هفته 3: SQL، EF Core و صحت داده

مرجع: ۱۱؛ ۱۲.۵.۳؛ ۳۰.۲؛ مکمل SQL و تراکنش.

| جلسه | موضوع | مطالعه | خروجی قابل بررسی |
|---|---|---|---|
| W3D1 | مدل داده | ۱۱.۵؛ ۲۹.۶ | ERD برای Note/Tag/Attachment/User؛ کلید، FK، constraint و مالکیت؛ مدل را با نیازمندی تطبیق بده. |
| W3D2 | SQL کاربردی | مکمل SQL | JOIN، GROUP BY، index و transaction؛ سه query را دستی بنویس و خروجی را توضیح بده. |
| W3D3 | EF و Migration | ۱۱.۴، ۱۱.۸؛ ۳۰.۲ | یک provider رابطه‌ای انتخاب کن؛ migration و seed روی DB تازه؛ تغییر schema را روی داده موجود تمرین کن. |
| W3D4 | خواندن مؤثر | ۱۱.۷ | projection، tracking و N+1؛ SQL تولیدشده و تعداد query را ثبت و مقایسه کن. |
| W3D5 | هم‌زمانی و تراکنش | ۱۲.۵.۳؛ مکمل | تست دو ویرایش هم‌زمان و شکست میانه عملیات؛ سیاست conflict و مرز transaction را ثبت کن. |
| W3D6 | تست دیتابیس | ۲۵.۳؛ مکمل provider واقعی | سناریوهای constraint، query و concurrency روی provider انتخابی؛ تست صرفاً EF InMemory شاهد رفتار رابطه‌ای نیست. |

روز هفتم / G3: SQL یک query مهم را بخوان؛ N+1 را اصلاح کن؛ دو ویرایش هم‌زمان نباید بی‌صدا یکدیگر را پاک کنند؛ migration روی دیتابیس تازه اجرا شود.

## هفته 4: امنیت و مالکیت داده

مرجع: ۱۰؛ ۱۲.۶؛ ۱۵ منتخب؛ ۳۰.۳.

| جلسه | موضوع | مطالعه | خروجی قابل بررسی |
|---|---|---|---|
| W4D1 | مدل تهدید | ۱۰؛ مکمل threat modeling | دارایی‌ها، مرزهای اعتماد و مسیرهای سوءاستفاده NoteKeeper؛ تفاوت authentication و authorization. |
| W4D2 | هویت و نشست | ۱۰ | انتخاب سازوکار استاندارد هویت و توضیح cookie/token؛ تست ورود معتبر و نامعتبر؛ secrets بیرون repo. |
| W4D3 | Authorization | ۱۰؛ ۱۲.۶ | مالکیت Note در read/update/delete؛ policy و تست دسترسی بین دو کاربر؛ صرف داشتن login کافی نیست. |
| W4D4 | مرز HTTP | ۱۲.۶ | CORS، CSRF متناسب با auth انتخابی، HTTPS و rate limiting؛ هر تنظیم با سناریوی مشخص توجیه شود. |
| W4D5 | فایل امن | ۱۵ منتخب | حد حجم، نام امن، storage خارج مسیر اجرای محتوا، مجوز download؛ یک آپلود و دانلود مالک‌محور ساده. |
| W4D6 | بازبینی امنیتی | مرور هفته | کد Agent را برای overposting، دسترسی افقی و log حاوی اطلاعات حساس بررسی کن؛ یافته‌ها و regression test ثبت شوند. |

روز هفتم / G4: کاربر A نتواند Note یا فایل کاربر B را با حدس ID بخواند/تغییر دهد؛ تست‌های anonymous، forbidden و مالک مجاز بگذرند.

## هفته 5: معماری و قضاوت مهندسی

مرجع: ۲۹.۳–۲۹.۷؛ ۳۰.۲–۳۰.۴؛ ۱۸.۷ منتخب؛ مکمل معماری.

| جلسه | موضوع | مطالعه | خروجی قابل بررسی |
|---|---|---|---|
| W5D1 | خواندن معماری کتاب | ۲۹.۳ | Brokers/Services/Orchestrations/Exposers را روی یک use case ردیابی کن؛ The Standard انتخاب کتاب است، الزام عمومی ASP.NET نیست. |
| W5D2 | مقایسه معماری | مکمل | یک slice کوچک را با رویکرد feature-based مقایسه کن؛ هزینه layered/vertical slice و تصمیم monolith را بنویس. |
| W5D3 | قوانین دامنه | ۳۰.۳ | قانون مالکیت/ویرایش را از transport جدا کن؛ وابستگی‌ها و مرز transaction مشخص باشد. |
| W5D4 | ADR | ۲۹؛ مکمل | سه تصمیم: ساختار، DB، auth؛ برای هرکدام مسئله، گزینه‌ها، انتخاب، پیامد و زمان بازنگری بنویس. |
| W5D5 | سناریوی تغییر | مکمل system design | افزودن اشتراک‌گذاری Note را فقط طراحی کن؛ تغییر مجوز، schema و API را مشخص کن؛ scope پیاده‌سازی را افزایش نده. |
| W5D6 | Refactor محدود | ۳۰.۲–۳۰.۴ | یک refactor کوچک بدون تغییر رفتار با Agent؛ قبل/بعد dependency map و تست؛ از بازنویسی کل پروژه پرهیز کن. |

روز هفتم / G5: با یک سناریوی تغییر، مرزها را دفاع کن؛ سه ADR با گزینه جایگزین و هزینه داشته باش؛ توضیح بده کدام abstraction واقعاً لازم است.

## هفته 6: مشاهده‌پذیری و کارایی

مرجع: ۹، ۱۴، ۲۶، ۲۸ منتخب؛ مکمل تاب‌آوری.

| جلسه | موضوع | مطالعه | خروجی قابل بررسی |
|---|---|---|---|
| W6D1 | Logging | ۹ | structured logs و correlation ID؛ یک خطای چندمرحله‌ای را از log دنبال کن؛ داده حساس را حذف کن. |
| W6D2 | اندازه‌گیری پایه | ۲۶.۱ و ۲۶.۴ | سناریوی بار، dataset، محیط و warmup ثابت؛ ثبت p50/p95، throughput و نرخ خطا، بدون ادعای ظرفیت production. |
| W6D3 | رفع گلوگاه | ۲۶.۲ | profiling ساده و query timings؛ یک bottleneck واقعی را رفع و همان سناریو را دوباره اجرا کن. |
| W6D4 | Caching | ۱۴؛ ۲۶.۳ | یک read پرتکرار، TTL و invalidation؛ تست تغییر داده و جداسازی مالک؛ اگر فایده اندازه‌گیری نشد cache را توجیه نکن. |
| W6D5 | تاب‌آوری | مکمل؛ ۲۸ منتخب | timeout، cancellation و retry محدود؛ توضیح خطر retry عملیات نوشتن و idempotency؛ dependency کند را شبیه‌سازی کن. |
| W6D6 | گزارش مهندسی | مرور هفته | گزارش قبل/بعد همراه محدودیت آزمایش؛ Agent راهکار بدهد، تو شواهد و trade-off را بررسی کن. |

روز هفتم / G6: یک کندی را با داده تشخیص بده؛ p95 و خطا قبل/بعد با بار و محیط ثابت ثبت شود؛ cache داده کاربران را مخلوط نکند.

## هفته 7: تست، تحویل و عملیات

مرجع: ۲۵، ۲۷؛ ۳۰.۸ منتخب؛ مکمل CI و Docker.

| جلسه | موضوع | مطالعه | خروجی قابل بررسی |
|---|---|---|---|
| W7D1 | بازبینی مجموعه تست | ۲۵ | نقشه ریسک به تست؛ happy path، failure، auth و concurrency؛ تست‌های صرفاً تقلید پیاده‌سازی را اصلاح کن. |
| W7D2 | تست رگرسیون | ۲۵.۳؛ ۳۰.۸ | سه خرابی عمدی در شاخه تمرینی؛ تست باید آن‌ها را کشف کند؛ زمان و نتیجه را ثبت کن. |
| W7D3 | CI | ۲۵.۵؛ مکمل | build/test در CI با DB تست؛ secret management و failure قابل فهم؛ در نبود remote اجرای معادل محلی را جدا برچسب بزن. |
| W7D4 | بسته‌بندی | ۲۷.۱–۲۷.۲ | Docker و تنظیمات محیط؛ health check؛ اجرای API و DB محلی با دستور مستند. |
| W7D5 | Migration و بازیابی | ۲۷.۳؛ مکمل | روی محیط تمرینی backup/restore و migration rehearsal؛ rollback کد لزوماً rollback داده نیست. |
| W7D6 | Release rehearsal | ۲۷ | از مسیر تازه اجرا کن؛ smoke test و runbook خرابی؛ استقرار عمومی/هزینه‌دار جزو این تمرین الزامی نیست. |

روز هفتم / G7: پروژه از checkout تازه طبق README اجرا و تست شود؛ CI خطا را بگیرد؛ migration، health check و روش rollback/restore مستند باشد.

## هفته 8: نمونه‌کار و آمادگی مصاحبه

مرجع: ۲۹–۳۰ مرور بک‌اند؛ مکمل مصاحبه، الگوریتم و system design.

| جلسه | موضوع | مطالعه | خروجی قابل بررسی |
|---|---|---|---|
| W8D1 | تکمیل نمونه‌کار | ۲۹–۳۰ مرور | بستن باگ‌های مهم؛ README شامل مسئله، اجرا، معماری، تست، محدودیت‌ها و نقش Agent؛ بدون افزودن قابلیت بزرگ. |
| W8D2 | مصاحبه بک‌اند | مرور ۴،۶،۱۰،۱۱،۱۲ | توضیح request lifecycle، DI، async، SQL و authorization با مثال پروژه؛ ندانسته‌ها ثبت شود. |
| W8D3 | System design | مکمل | طراحی سرویس یادداشت برای افزایش بار: فرض بار، DB، cache، storage، failure و trade-off؛ عددها را فرض اعلام کن. |
| W8D4 | Debug و Code review | مرور | باگ ناشناخته و PR دارای اشکال امنیت/هم‌زمانی؛ قبل از درخواست کمک فرضیه و شاهد بنویس. |
| W8D5 | رزومه و شواهد | مکمل اپلای | سه bullet فقط بر اساس دستاورد واقعی؛ دو داستان STAR؛ تطبیق با ۳ آگهی منتخب پس از مشخص شدن بازار هدف. |
| W8D6 | ارزیابی نهایی | همه محورهای اصلی | دمو و مصاحبه آزمایشی؛ نمره مستقل از Agent؛ برنامه جبرانی برای محورهای زیر حد لازم. |

روز هفتم / G8: دموی ۱۰ دقیقه‌ای، مصاحبه فنی و طراحی بدون Agent، PR review و رفع باگ ناشناخته؛ شکاف‌های باقی‌مانده صریح ثبت شوند.

## Software Engineer بودن در طول مسیر

در هفته ۱ و ۲ مسئول روشن‌کردن مسئله و قرارداد هستی؛ در هفته ۳ و ۴ مسئول درست‌ماندن داده و مجوزها؛ در هفته ۵ مسئول هزینه تصمیم‌های معماری؛ در هفته ۶ و ۷ مسئول فهم رفتار سیستم و قابلیت تحویل؛ در هفته ۸ مسئول توضیح صادقانه شواهد و محدودیت‌ها. Mid-level مورد هدف این برنامه کسی است که یک قابلیت با دامنه مشخص را با استقلال نسبی پیش می‌برد، ابهام را روشن می‌کند، ریسک را می‌بیند و در زمان مناسب کمک می‌گیرد. تعداد فریم‌ورک‌های حفظ‌شده یا تعداد خطوط تولیدشده معیار کافی نیست.

## معیار تسلط و پایان مسیر

برای هر جلسه چهار امتیاز ۰ تا ۳: توضیح، اجرا/رفع باگ، ارزیابی/تست، دفاع از تصمیم. ۰=هنوز ارزیابی نشده یا ناتوان؛ ۱=با راهنمایی زیاد؛ ۲=مستقل در سناریوی آشنا؛ ۳=مستقل در سناریوی تازه و توضیح trade-off. «verified» فقط وقتی هر چهار محور حداقل ۲ دارند و شاهد ثبت شده است؛ self-report مطالعه کافی نیست. برای امنیت و صحت داده، شکست سناریوی بحرانی مانع عبور است حتی اگر میانگین خوب باشد.

مرور هر جلسه تأییدشده ۱، ۷ و ۲۱ روز بعد از تاریخ تأیید ثبت می‌شود؛ پاسخ ضعیف وضعیت را needs_review می‌کند. فرمول پیشرفت اجرای برنامه: جلسات verified / ۴۸؛ درصد حضور جداست. ارزیابی‌های هفتگی ۰ تا ۸ جدا گزارش می‌شوند و این درصد به معنای درصد آمادگی استخدام نیست. آمادگی اپلای پس از ارزیابی نهایی در شش محور C#/HTTP، داده، امنیت، معماری، تست/عملیات و حل مسئله گزارش می‌شود.

## منابع مکمل با دامنه محدود

SQL، الگوریتم، طراحی سیستم، فرایند Git/PR، CI و مهندسی Agent مکمل این پلن هستند و نباید پوشش کامل آن‌ها را به کتاب نسبت داد. برای جزئیات API و تفاوت نسخه‌ها از [مستندات ASP.NET Core 10](https://learn.microsoft.com/en-us/aspnet/core/fundamentals/?view=aspnetcore-10.0) استفاده می‌کنیم؛ کد کتاب یا Agent نیز با نسخه نصب‌شده build/test می‌شود. فهرست کتاب برنامه مرجع است، نه اثبات صحت هر قطعه کد.
